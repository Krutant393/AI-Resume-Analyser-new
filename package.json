{
  "name": "ai-resume-analyser-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack AI Resume Analyser and ATS scoring application",
  "scripts": {
    "build": "npm --prefix ai-resume-analyser install --no-save && npm --prefix ai-resume-analyser run build && npm --prefix Backend install --no-save",
    "start": "node Backend/server.js",
    "start:backend": "npm --prefix Backend start",
    "dev:frontend": "npm --prefix ai-resume-analyser run dev",
    "dev:backend": "npm --prefix Backend run dev"
  },
  "keywords": [
    "ai",
    "resume",
    "ats",
    "react",
    "express"
  ],
  "author": "",
  "license": "ISC"
}
